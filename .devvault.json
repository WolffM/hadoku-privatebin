{
  "// comment": "Maps env-var name -> vault key. Loaded by ../hadoku_site/scripts/secrets/dev-vault.mjs and read by services/pm2/privatebin-wrapper.mjs in hadoku_site. Values fetched at runtime, never committed. No tier key arrays: the shim trusts edge-router for tier resolution (it forwards X-User-Key/X-User-Id untouched). `key-generate --repo ../hadoku-privatebin` grants this repo's service key ACL rows for exactly these keys.",

  "PRIVATEBIN_PORT": "PRIVATEBIN_PORT",
  "PRIVATEBIN_BACKEND_URL": "PRIVATEBIN_BACKEND_URL"
}
